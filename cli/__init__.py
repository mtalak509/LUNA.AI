"""Консольный CLI-харнесс для ручной обкатки MainAgent (без HTTP/оркестратора).

Пакет самодостаточен: сборка агента берётся напрямую из `core`.
Точка входа — `python -m cli` (или консольная команда `luna`).
"""
